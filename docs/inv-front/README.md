
  # Extract UI Functions

  This is a code bundle for Extract UI Functions. The original project is available at https://www.figma.com/design/MyfTMNf1Q6INpID90wRBYu/Extract-UI-Functions.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  