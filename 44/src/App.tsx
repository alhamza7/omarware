import { PosAppContainer } from './features/pos/containers/PosAppContainer';

/** Entry point — renders the fully API-connected POS. */
export default function App() {
  return <PosAppContainer />;
}
