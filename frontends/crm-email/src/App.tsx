import { EmailAppContainer } from './features/email/containers/EmailAppContainer';

/** Entry point — renders the fully API-connected email client. */
export default function App() {
  return <EmailAppContainer />;
}
