<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <!-- SAP Sale Sync Tree View -->
        <record id="view_sap_sale_sync_tree" model="ir.ui.view">
            <field name="name">sap.sale.sync.tree</field>
            <field name="model">sap.sale.sync</field>
            <field name="arch" type="xml">
                <list string="Sale Sync" 
                      decoration-success="sync_status == 'success'" 
                      decoration-danger="sync_status == 'error'">
                    <field name="backend_id"/>
                    <field name="odoo_sale_id"/>
                    <field name="sap_sale_id"/>
                    <field name="sap_doc_entry"/>
                    <field name="sync_status" widget="badge"/>
                    <field name="sync_direction"/>
                    <field name="last_sync"/>
                </list>
            </field>
        </record>
        
        <!-- SAP Sale Sync Form View -->
        <record id="view_sap_sale_sync_form" model="ir.ui.view">
            <field name="name">sap.sale.sync.form</field>
            <field name="model">sap.sale.sync</field>
            <field name="arch" type="xml">
                <form string="Sale Sync">
                    <header>
                        <field name="sync_status" widget="statusbar"/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="backend_id"/>
                                <field name="odoo_sale_id"/>
                                <field name="sap_sale_id"/>
                                <field name="sap_doc_entry"/>
                            </group>
                            <group>
                                <field name="sync_direction"/>
                                <field name="last_sync"/>
                                <field name="retry_count"/>
                                <field name="max_retries"/>
                            </group>
                        </group>
                        <group string="Error" invisible="not error_message">
                            <field name="error_message"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        
        <!-- SAP Sale Sync Action -->
        <record id="action_sap_sale_sync" model="ir.actions.act_window">
            <field name="name">Sale Sync</field>
            <field name="res_model">sap.sale.sync</field>
            <field name="view_mode">list,form</field>
        </record>
        
        <!-- Menu Item -->
        <menuitem id="menu_sap_sale_sync"
                  name="Sale Sync"
                  parent="menu_sap_integration"
                  action="action_sap_sale_sync"
                  sequence="60"/>
        
    </data>
</odoo>





